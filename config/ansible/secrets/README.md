Store encrypted secrets here.

- Copy sample files from `../group_vars/all/vault.sample.yml` or create new YAML.
- Encrypt the real file with `ansible-vault encrypt`.
- Do not commit real secrets; this folder is ignored except for this README.

